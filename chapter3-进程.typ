#import "@local/ysz_tools:0.1.0": *
#import "@preview/cuti:0.4.0": *
#show: show-cn-fakebold

#let pointbox(title: "重点与常识", body) = block(
  fill: rgb("#fff5f5"),
  stroke: (left: 4pt + rgb("#fa5252")),
  inset: 12pt,
  radius: 4pt,
  width: 100%,
  spacing: 1.5em,
  [
    #text(weight: "bold", fill: rgb("#c92a2a"))[#title] \
    #v(4pt)
    #body
  ],
)

#let img(path, caption: "", width: 80%, supplement: [图]) = {
  figure(
    image(path, width: width),
    caption: caption,
    supplement: supplement,
    kind: image,
  )
}
#show: conf.with(
  title: " OS note",
  author: "kiwiizzz & xhkzdepatedream & beigou9",
)
#show "（": "("
#show "）": ")"
#show ",": "，"
#show ":": "："

= 进程
#img("assets/chapter3/进程内存布局示意图.png", width: 100%, caption: [
  一个进程包括:代码段(text),程序计数器和处理机寄存器内容,栈,数据段
])

#table(
  columns: (auto, 1fr, 1fr),
  inset: 10pt,
  align: horizon,
  fill: (x, y) => if y == 0 { gray.lighten(80%) },
  stroke: 0.5pt + gray,

  [*对比维度*], [*程序 (Program)*], [*进程 (Process)*],

  [本质概念], [静态概念：指令的有序集合。], [动态概念：程序在处理机上的一次执行过程。],

  [存在形式], [可长久保存（如在硬盘上）。], [暂时的，有生命周期（创建→执行→暂停→消亡）。],

  [状态变化], [无状态变化，只是代码文件。], [是一个状态不断变化的过程（就绪、运行、阻塞等）。],

  [组成结构],
  [仅包含代码（指令）。],
  [进程映像 = 程序段 + 数据段 + 进程控制块 (PCB)。\ #emph[*(注：PCB是进程存在的唯一标志)*]],

  [对应关系],
  [一个程序可对应多个进程 \ #text(size: 8pt)[(例：打开多个Chrome窗口)]],
  [一个进程可包含多个程序 \ #text(size: 8pt)[(例：执行脚本时调用其他子程序)]],

  [主动性], [被动实体，不能主动创建新程序。], [主动实体，可以创建新进程 (如 `fork`)。],

  [资源视角], [不占用系统运行资源（仅在存储时占空间）。], [是资源分配和调度的基本单位（占用CPU、内存等）。],
)


#img("assets/chapter3/进程状态转换图.png", caption: [
  进程的状态
])

中断:典型如时钟，也可能其它来源如鼠标，OS借机夺回CPU，趁机切换进程。但并不总引起切换，如鼠标移动，触发内核中断服 务，但处理完成后决定回到原进程，不切换 
// 这个图我来负责

== 因果变迁
如果一个进程的状态变化A的发生，会引起另一个进程的状态变化B的发生，则称A、B之间是因果变迁

#definition("挂起")[
  进程挂起也称为 *静止状态*，是相对于正常 *活动状态* 的一种特殊表现。其核心特征包括：
- *被动性*：由外部（如 OS 或其他进程）强制剥夺执行权，且不改变进程原有的就绪或阻塞逻辑状态。
- *维度共存*：就绪/阻塞反映进程的“健康状态”（执行逻辑），而挂起/活动反映进程的“人身自由状态”（资源调度）。两者互不冲突，可并存。

*挂起诱因 (Reasons for Suspension)*：
1. *系统维护*：发生故障或功能受损时，挂起进程以进行修复。
2. *中间检查*：为观察或调试目的，挂起进程以检查中间运行结果。
3. *资源调配*：资源不足时，挂起部分进程以腾出系统资源。
4. *内存优化*：内存告急时，将进程挂起并对换至外存（Swap），释放内存空间。
]
挂起的引入会让之前的进程图变得越来越复杂.//还有一个图



== 进程控制块 PCB，Process Control Block
#definition("进程控制块")[
  PCB 是操作系统为管理进程而专门定义的一种数据结构，记录了进程执行时的全部动态信息。
- *地位*：PCB 是进程存在的 *唯一标志*。系统通过感知 PCB 来感知进程，而非程序代码。
- *生命周期*：创建进程时申请 PCB，进程结束时释放 PCB。
]

#img("assets/image.png",caption:[
  不同操作系统的PCB结构不同,但通常应该包括下面的内容.
])


#table(
  columns: 2,
  stroke: 1pt,
  inset: 8pt,
  
  [*字段名称*, *作用*],
  
  [进程标识符 (Process Identifier, PID), [唯一标识进程，包括PID、PPID、UID等]],
  
  [进程状态 (Process State), [记录进程当前所处状态（新建、就绪、运行、阻塞、终止）]],
  
  [程序计数器 (Program Counter, PC), [指向进程下一条要执行的指令地址]],
  
  [CPU寄存器 (CPU Registers), [保存通用寄存器、累加器、栈指针等现场信息]],
  
  [程序状态字 (Program Status Word, PSW), [保存条件码、执行模式、中断屏蔽标志等]],
  
  [进程队列指针 (Queue Pointers), [指向下一个PCB地址，用于构建就绪/阻塞队列链表]],
  
  [内存管理信息 (Memory Management Information), [记录基址寄存器、界限寄存器、页表/段表、程序和数据地址]],
  
  [进程优先级 (Process Priority), [决定进程获得CPU的优先程度]],
  
  [调度信息 (Scheduling Information), [包含调度策略、时间片、CPU使用时间等]],
  
  [资源清单 (Resource List), [列出已分配资源（打开文件、I/O设备、信号量等）]],
  
  [通信信息 (Inter-Process Communication, IPC), [记录进程间通信相关数据（消息队列、管道、共享内存等）]],
  
  [家族关系 (Parent/Child Relationship), [指明父子进程关系，用于进程管理和信号传递]],
  
  [记账信息 (Accounting Information), [记录CPU使用时间、创建时间、资源使用统计等]]
)

  
操作系统通常将多个 PCB 以特定的数据结构组织起来：
- *链接方式*：按状态将 PCB 分为多个队列（如就绪队列、阻塞队列）。
- *索引方式*：按状态建立索引表，表项指向 PCB 的起始地址。

*PCB 的作用总结*
- *独立运行*：使多道程序环境下，程序能作为独立单位并发执行。
- *间断执行*：保存 CPU 现场，支持进程在切换后能从断点继续运行。
- *资源管理*：记录进程所需资源，防止系统资源冲突。

=== PCA的组织方式
#grid(
  columns: 2,
  img("assets/image-1.png",caption:[
  ?
]),
img("assets/image-3.png",caption:[
  ?
]),
)

== 进程调度
#img("assets/image-5.png",caption:[
  就绪队列：可以一个，但通常每个CPU一个 \
  阻塞队列：每种阻塞原因一个以方便唤醒时查找 

])<process_queue>
队列里存的不是进程的代码或数据，而是PCB。
+ 就绪队列 (Ready Queue)：
  - 定义： 所有处于“就绪态”进程的 PCB 集合。
  - 含义： 这些进程万事俱备（内存已加载、资源已申请），只欠东风（CPU）。只要调度器选中它，它就能上 CPU 运行。
  - 图示： 上方的 ready queue，通过 head 和 tail 指针，把 PCB_7 和 PCB_2 串在了一起。
+ 阻塞队列 (Blocked / Wait Queue)：
  - 定义： 所有处于“阻塞态”（等待态）进程的 PCB 构成的队列。
  - 含义： 这些进程因为某些原因（比如等待 I/O 完成、等待信号量）暂时无法运行，必须让出 CPU 去排队等待。
  - 图示： 下方的 wait queue，把 PCB_3、PCB_14 等串在了一起。

#note[
  每种阻塞原因都有一个阻塞队列。

  如果所有等待 I/O 的进程都挤在一个巨大的“阻塞队列”里。当磁盘读写完成了，操作系统需要唤醒进程时，它就得遍历整个大队列，找出“哦，原来是你在等磁盘”。这太慢了。

  优化：如果按“阻塞原因”分类，比如“等待磁带的队列”、“等待磁盘的队列”、“等待键盘输入的队列”。当磁盘完成了，操作系统直接去“磁盘等待队列”里把人捞出来唤醒，速度极快。

  @process_queue 说明：你可以看到下面分出了 mag tape unit 0（磁带机0）、disk unit 0（磁盘0）、terminal unit 0（终端0）等多个队列。
]


// cpu schedule
=== 1. 长程调度 (Long-term Scheduling / Job Scheduling)
- *核心职能*：从 *后备队列*（磁盘）中选择作业调入内存，为其创建 PCB，使其变为 *就绪进程*。
- *角色定位*：内存的“守门员”。
- *控制参数*：控制 *多道程序度 (Degree of Multiprogramming)*，即内存中同时存在的进程数量。
- *任务差异*：
  - *计算任务*：接受作业调度（排队入场）。
  - *交互任务*：不经过此阶段，必须立刻启动，“生来就是进程”。
- *系统分布*：多道批处理系统必备；分时系统（如现代 Linux/Windows）可能存在，但通常较弱。

=== 2. 短程调度 (Short-term Scheduling / CPU Scheduling)
- *核心职能*：从 *就绪队列* 中选择下一个获得 CPU 执行权的进程。
- *角色定位*：CPU 的“分配器”。
- *频率*：极高（毫秒级），必须非常精简高效。
- *系统分布*：*所有* 类型的操作系统（批处理、分时、实时）都必然存在。


=== 上下文切换
为了提高处理器的利用率，操作系统需要让处理器足够忙，即让不同的程序轮流占用处理器来运行。如果一个程序因某个事件而不能运行下去时，就通过进程上下文切换把处理器占用权转交给另一个可运行程序。
#img("assets/image-6.png", width: 50%, caption: [
  进程上下文切换
])

#remark("两种切换的讨论")[
  #img("assets/image-7.png", width: 60%, caption: [
    两种切换
  ])<two_switch>
  OS在内核中为每个进程准备了一个栈，称为该进程的内核栈。

  用户栈： 进程在用户态运行时（比如你在写代码、玩游戏），用的是用户栈（通常在内存低地址）。

  内核栈： 当进程因为系统调用（比如请求读写文件）或者中断（比如时间片到了）进入内核态时，CPU 会切换到这个进程专属的内核栈（通常在内存高地址）上来运行操作系统的代码。

  两种切换都导致程序的跳转。纵向：内核态与用户态的切换。横向：进程间的切换。
  
  + 只要能从内核态回到用户态，那么内核栈一定是空的。
  
      当你准备离开内核态，回到用户态继续干活时，这次进入内核态所产生的一切“临时数据”都必须*清理干净*。例如，进程 P0 在用户态运行得好好的，突然发生了一个中断（比如键盘输入）。CPU 必须暂停 P0 的用户程序，跳转到内核代码去处理。CPU 把当前的程序计数器（PC）、寄存器值等“现场信息”压入 P0 的内核栈中保存。

  + 只有在内核态才会发生横向的切换。

    - @two_switch 的用户态不能直接横向跳到 P1（红色叉叉），因为用户程序没权限。
    - P0 的用户态也不能直接跳到 P1 的内核态（红色叉叉），因为那是非法的入口。
    - *唯一的路径*： P0 用户态 -> (纵向) -> P0 内核态 -> (横向，可选) -> P1 内核态 -> (纵向) -> P1 用户态。
]

== 进程状态与转换
进程在执行时会不断改变状态，主要包括五种基本状态：新建、运行、等待（阻塞/睡眠）、就绪、终止。
- *就绪态*：已获得除CPU以外的所有资源，一旦分配了处理机就可以立即执行。
- *等待态（阻塞）*：由于发生某事件（如等待I/O）而暂时无法执行，即使分配CPU也无法运行。

#pointbox(title: "挂起状态 (Suspended)")[
  - 挂起也称*静止状态*，是被外界（OS或其它进程）强制剥夺执行权的被动状态。
  - 挂起的原因包括：系统故障修复、检查中间结果、资源不足、内存不足（换出到外存）。
]

// == 进程控制块 (PCB)
// #definition(title: "Process Control Block (PCB)")[
//   - PCB是OS内核中描述和管理进程的数据结构，是进程实体的核心部分。
//   - 操作系统通过PCB感知进程的存在，PCB是进程存在的唯一标志。
// ]
// - *包含内容*：进程标识符、当前状态、队列指针、程序和数据地址、进程优先级、CPU现场保护区、通信信息、家族关系、资源清单等。
// - *组织方式*：线性方式、链表方式（按状态组成就绪队列、阻塞队列等）、索引方式。

=== 进程的操作与原语
创建、撤消、阻塞、唤醒等,一般由操作系统内核*原语*实现。仅适用于单 CPU，多 CPU 时，其他 CPU 仍可同时访问共享资源 → 需要自旋锁 + 原子指令解决

原语实现：开头关中断，完成后重开中断。关中断是特殊指令，使CPU不理会外部中断。中断仍然会产生，只是CPU不理会；能屏蔽的中断仅限外部中断（包括定时器），内部异常（如除零错误、页故障）通常不受影响，因为它们与当前指令执行直接相关，必须立即处理。

在单CPU系统中，进程调度的本质是由定时器中断触发的。
如果关了中断（包括定时器），那么即使时间片到了，也不会发生中断，也就不会触发调度器，当前进程就不会被剥夺CPU。因此，在关中断期间，当前进程独占CPU，不会被抢占，从而保证了原语操作的“原子性”。

开关中断是特权指令 → 只能在内核态执行。

+ 创建:通过创建进程系统调用可以创建新进程，创建方称为父进程，被创建方称为子进程.进程的创建需要OS执行，但多数进程都是在现有进程的要求下创建的.例如Linux的bash进程创建vim进程。
  + 触发原因：
    - 用户登录 → 创建 shell 进程
    - 作业调度 → 为作业分配资源并创建进程
    - OS 服务 → 创建守护进程（如 cron）
    - 应用需求 → 父进程调用 fork() 创建子进程（如 bash → vim）
  + 创建过程（原语步骤）：
    + 申请空闲 PCB
    + 分配资源（内存、文件句柄等）
    + 初始化 PCB（填名字、优先级、地址空间、状态等）
    + 插入就绪队列
  + 父子关系：父进程创建子进程 → 形成进程树
  + 资源策略三种：
    - 共享所有资源（少见）
    - 共享子集（常见，如 Linux fork 后写时复制）
    - 无共享（完全独立）
  + 执行方式：
    - 并发执行（默认）
    - 父等待子结束（如 wait()）
#remark[
  “进程创建 = 申请 PCB + 分配资源 + 初始化 + 入队；父子可共享资源，也可独立；通过系统调用触发”
]

#import "@preview/cetz:0.4.2"

#let f = cetz.canvas({
  import cetz.draw: *

  // 设置全局线宽和圆角，模拟手绘的柔和感
  let frame-style = (stroke: 1.5pt + black, fill: white, radius: .1)

  // 1. 右侧的 Bash 进程 (触发源)
  group(name: "bash-group", {
    rect((5, 0), (9, 3), ..frame-style, name: "bash-box")
    content("bash-box.north", padding: .2, anchor: "south")[*bash*]
    
    // 模拟终端提示符和命令
    content((5.5, 2.3), anchor: "west", size: 15pt)[`# vim `]
    
    // 那个 L 型的回车箭头 (Enter)
    line((7.8, 2.1), (7.8, 1.8), (7.4, 1.8), mark: (end: "stealth", fill: black))
    
    // 标注：此时 Bash 进入阻塞状态
    content("bash-box.south", padding: .3, anchor: "north")[
      #set text(size: 9pt, fill: gray.darken(20%))
      调用 `wait()` \
      进程挂起 (Blocked)
    ]
  })

  // 2. 中间的指向箭头 (控制权转移/系统调用)
  // 使用双线或加粗箭头表示 Fork/Exec
  line((4.8, 1.5), (3.2, 1.5), mark: (end: "stealth", size: .4), stroke: 2pt)
  content((4, 1.8), size: 9pt)[*fork exec*]

  // 3. 左侧的 Vim 进程 (子进程)
  group(name: "vim-group", {
    rect((0, 0), (3, 3), ..frame-style, name: "vim-box")
    content("vim-box.north", padding: .2, anchor: "south")[*vim*]
    
    // 模拟文件内容线条
    for i in range(1, 5) {
      line((0.5, 2.5 - i * 0.4), (2.5, 2.5 - i * 0.4), stroke: gray + 1pt)
    }
    
    // 模拟底部的命令状态栏
    content((0.2, 0.4), anchor: "west", size: 10pt)[`:`]
    
    content("vim-box.south", padding: .3, anchor: "north")[
      #set text(size: 9pt)
      子进程运行中 \
      独占终端 I/O
    ]
  })
})
#figure(
  f,caption:[
    例子
  ]
)

注意:有些系统中，若父进程终止，不允许子进程继续(这被称为级联终止).

阻塞和就绪是描述自己能不能被执行,挂起是描述(外界)强制要求自己不能执行.

进程创建 = 申请 PCB + 分配资源 + 初始化 + 入队；父子可共享资源，也可独立；通过系统调用触发
=== Linux案例
#img("assets/image-8.png",caption:[
  进程树e
])